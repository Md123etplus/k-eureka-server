server:
  port: 8761  # Eureka runs on port 8761

eureka:
  client:
    register-with-eureka: false  # This is the server; it doesn't register itself
    fetch-registry: false        # Doesn't fetch other services (it's the registry)